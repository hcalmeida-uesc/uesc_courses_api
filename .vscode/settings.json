{
   "sqltools.connections": [
      {
         "previewLimit": 50,
         "driver": "SQLite",
         "name": "uesc_courses",
         "database": "${workspaceFolder:dev}/uesc_courses.db"
      }
   ]
}
